{% extends 'base.html.twig' %}
{% block body %}
	<div class="row">
		<div class="col-lg-12 text-center">
			<h1>Poker Test</h1>
		</div>
	</div>
	{% if upload is defined and upload == true %}

	{% else %}
	<form method="POST" action="/Upload" enctype="multipart/form-data">
		<div class="row">
			<div class="col-lg-5 col-sm-2"></div>
			<div class="col-lg-2 col-sm-2">
				<div class="form-group text-center">
					<label for="dataFile">Load data file</label>
					<input type="file" class="form-control-file" id="upload_file" name="upload_file">
				</div>
			</div>
			<div class="col-lg-5 col-sm-2"></div>
		</div>
		<div class="row">
			<div class="col-lg-12 text-center"><input type="submit" class="btn btn-success form-control" value="submit"></div>
		</div>
	</form>
	{% endif %}
{% endblock %}
